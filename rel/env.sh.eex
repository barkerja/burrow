#!/bin/sh

# Enable IPv6 for Fly.io private networking
export ERL_AFLAGS="-proto_dist inet6_tcp"
export ELIXIR_ERL_OPTIONS="-proto_dist inet6_tcp"

# Clustering configuration
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="${FLY_APP_NAME}@${FLY_PRIVATE_IP}"

# Cookie must be consistent across all nodes
# Set RELEASE_COOKIE as a Fly secret: fly secrets set RELEASE_COOKIE=$(openssl rand -hex 32)
